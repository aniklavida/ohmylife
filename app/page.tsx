// The home page — the summary, "what needs you", the areas, the tending record —
// is designed and built in a later step (see docs/ROADMAP.md, step 4 onward).
//
// This placeholder exists only so the Next.js app builds and runs. It renders no
// life data, real or invented: the file format and the rebuildable index landed
// first (docs/ROADMAP.md, step 1), and nothing here should imply otherwise.
export default function HomePage() {
  return (
    <main>
      <h1>OhMyLife</h1>
      <p>The interface has not been built yet. See docs/ROADMAP.md.</p>
    </main>
  );
}
